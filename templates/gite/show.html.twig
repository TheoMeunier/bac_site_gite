{% extends 'base.html.twig' %}

{% block title gite.name %}

{% block body %}

    <h1>coucou</h1>

    <div class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <img src="..." alt="">
                </div>
                <div class="col-md-4">
                    <h1>{{ gite.name }}</h1>
                    <h2>{{ gite.chambres }} chambres - {{ gite.surface }} m2</h2>
                    <div class="text-primary" style="font-size: 4em; font-weight: bold">{{ gite.price }}€
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}